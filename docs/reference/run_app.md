# Launch the RMSTdesign Shiny Application

A function to easily launch the interactive Shiny application bundled
with the package.

## Usage

``` r
run_app()
```

## Examples

``` r
if (FALSE) { # \dontrun{
  RMSTpowerBoost::run_app()
} # }
```
