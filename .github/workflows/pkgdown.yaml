# Workflow intentionally disabled.
# Use local pkgdown builds and commit docs/ on main. GitHub Pages serves /docs.

name: pkgdown

on:
  workflow_dispatch:

permissions: read-all

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build pkgdown locally and commit docs/ on main. GitHub Pages serves /docs."
