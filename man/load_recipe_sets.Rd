% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_sets_datagen.R
\name{load_recipe_sets}
\alias{load_recipe_sets}
\title{Load datasets from a recipe-sets manifest}
\usage{
load_recipe_sets(manifest_path)
}
\arguments{
\item{manifest_path}{Path to \code{manifest.rds}.}
}
\value{
A named list where each element is \code{list(data=..., meta=...)}.
}
\description{
Reads a \code{manifest.rds} created by \code{generate_recipe_sets()}, loads one
dataset per row (preferring \code{rds} to \code{rdata} to \code{csv} to \code{txt}),
restores attribute \code{"achieved_censoring"}, and returns a named list
of \code{list(data = <data.frame>, meta = <list>)}.
}
\examples{
covs <- list(covar_continuous("x", mean = 0, sd = 1))
rec <- recipe_quick_aft(
  n = 20, model = "aft_lognormal",
  baseline = list(mu = 2.7, sigma = 0.6),
  treat_effect = -0.2, covariates = covs,
  target_censoring = 0.2, seed = 123
)
out <- file.path(tempdir(), "rmst_load_recipe_sets")
generate_recipe_sets(rec, out_dir = out, formats = "rds", n_reps = 1)
sets <- load_recipe_sets(file.path(out, "manifest.rds"))
names(sets)
str(sets[[1]]$meta)
}
