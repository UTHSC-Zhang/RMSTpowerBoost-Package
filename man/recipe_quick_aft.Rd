% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe_convenience.R
\name{recipe_quick_aft}
\alias{recipe_quick_aft}
\title{Quick AFT recipe builder (list-only)}
\usage{
recipe_quick_aft(
  n,
  tau,
  model = c("aft_lognormal", "aft_weibull"),
  baseline,
  treat_effect,
  covariates,
  target_censoring = 0.25,
  allocation = "1:1",
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{tau}{Restriction time (analysis horizon).}

\item{model}{One of \code{"aft_lognormal"} or \code{"aft_weibull"}.}

\item{baseline}{Baseline parameter list (see model).}

\item{treat_effect}{Numeric treatment coefficient (on log-time scale).}

\item{covariates}{Covariate definitions (list of defs).}

\item{target_censoring}{Target overall censoring fraction (0-1).}

\item{allocation}{Allocation ratio string (e.g., "1:1").}

\item{seed}{Optional seed.}
}
\value{
A recipe list suitable for \code{\link{simulate_from_recipe}}.
}
\description{
Convenience constructor for common AFT scenarios (lognormal/Weibull).
}
\examples{
covs <- list(list(name="x", type="continuous", dist="normal", params=list(mean=0, sd=1)))
r <- recipe_quick_aft(120, 12, "aft_lognormal",
       baseline=list(mu=2.3, sigma=0.5), treat_effect=-0.2,
       covariates=covs, target_censoring=0.25, allocation="1:1")
dat <- simulate_from_recipe(r, seed = 1)
}
