% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst-dp.R
\name{simulate_dp_data}
\alias{simulate_dp_data}
\title{Simulate Data for Dynamic Prediction Power Calculation}
\usage{
simulate_dp_data(
  n_per_group,
  L,
  K,
  interval,
  base_rate_survival = 0.1,
  group_effect_Za = log(0.8),
  Zb_effect = 0.05,
  censoring_rate = 0.05,
  treatment_rate = 0.05
)
}
\arguments{
\item{n_per_group}{Numeric, the number of subjects in each binary group (total N = 2 * n_per_group).}

\item{L}{Numeric, the restriction time for RMST.}

\item{K}{Integer, the number of cross-sections.}

\item{interval}{Numeric, the time interval between consecutive cross-sections.}

\item{base_rate_survival}{Numeric, baseline hazard rate for survival events.}

\item{group_effect_Za}{Numeric, the log-hazard ratio (or linear effect) for \code{Za} (group effect)
on survival. This is the effect size to detect.}

\item{Zb_effect}{Numeric, the log-hazard ratio (or linear effect) for \code{Zb} on survival.}

\item{censoring_rate}{Numeric, baseline hazard rate for censoring.}

\item{treatment_rate}{Numeric, baseline hazard rate for treatment assignment.}
}
\value{
A list containing two data frames:
\item{dat}{Main data frame with \code{ID}, \code{entry}, \code{X}, \code{deltaD}, \code{deltaT}, \code{Za}, \code{Zb1} to \code{ZbK}.}
\item{datA}{Treatment assignment data frame with \code{ID}, \code{A}, \code{status}.}
}
\description{
This function generates synthetic survival and treatment data for dynamic prediction models.
It creates \code{dat} (baseline info, time-to-event, treatment status) and \code{datA} (treatment assignment times)
data frames, structured to be compatible with the \code{dp} function.
The \code{Za} covariate is designed to represent the binary group for which power is calculated.
}
\keyword{internal}
